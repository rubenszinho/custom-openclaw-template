# ============================================================================
# MIDDLEWARE API CONFIGURATION
# ============================================================================
# IMPORTANT: This is the URL of your BACKEND API (a separate service),
# NOT the URL where this OpenClaw bot is deployed.
# 
# Example: If your backend API is deployed at:
#   https://my-backend-api.example.com
# Then set MIDDLEWARE_URL to that URL (no trailing slash)
MIDDLEWARE_URL=https://your-api.example.com

# Authentication token for your backend API
API_KEY=your-secret-api-key

# ============================================================================
# PLATFORM INTEGRATIONS (OPTIONAL)
# Uncomment and configure ONLY the platforms you want to use
# You need at least ONE platform, but can enable multiple
# ============================================================================

# --- Discord ---
# Get token from: https://discord.com/developers/applications
# Required Gateway Intents: MESSAGE_CONTENT, GUILD_MESSAGES, DIRECT_MESSAGES
# DISCORD_BOT_TOKEN=your-discord-bot-token

# --- Telegram ---
# Get token from: @BotFather on Telegram
# Create bot with: /newbot
# TELEGRAM_BOT_TOKEN=your-telegram-bot-token

# --- WhatsApp ---
# Uses WhatsApp Web protocol (QR code login)
# No token needed - configure via OpenClaw CLI: `openclaw channels login`
# Just deploy and run: openclaw channels login
# WHATSAPP_ENABLED=true

# --- Slack ---
# Get token from: https://api.slack.com/apps
# Required OAuth Scopes: chat:write, channels:history, im:history
# SLACK_BOT_TOKEN=your-slack-bot-token
# SLACK_APP_TOKEN=your-slack-app-token

# --- Mattermost ---
# Deploy as a Mattermost plugin
# See: https://docs.openclaw.ai/channels/mattermost
# MATTERMOST_URL=https://your-mattermost.com
# MATTERMOST_TOKEN=your-mattermost-token

# ============================================================================
# LLM PROVIDER CONFIGURATION
# ============================================================================
# OpenAI API key (or other LLM provider supported by OpenClaw)
OPENAI_API_KEY=sk-your-openai-key

# Anthropic Claude (alternative to OpenAI)
# ANTHROPIC_API_KEY=your-anthropic-key

# ============================================================================
# OPTIONAL CONFIGURATION
# ============================================================================

# Wrapper server port (Railway will set this automatically)
PORT=8080

# Gateway authentication token
# Access dashboard with: https://your-domain.com/?token=<value>
# The wrapper server proxies to OpenClaw's internal gateway
OPENCLAW_GATEWAY_TOKEN=your-secure-token-here

# LOG_LEVEL=info
# REQUEST_TIMEOUT=30
# MAX_RETRIES=3
